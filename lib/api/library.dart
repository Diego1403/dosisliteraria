part of 'api.dart';

extension Library on API {
  /* Future<Library> library() async {
    final json = (await get('/library')).body;
    return Library.fromJson(json);
  } */

  /* TODO Implement daily snippet
  Future<Library> dailySnippet() async {
    return Library.fromJson(await get('/daily/snippet', credentials));
  }
  */
}
